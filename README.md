# AiPIWatch

HTTP request logging and analytics REST API built with Spring Boot.

## How to run

- Build: `mvn clean package`
- Run: `mvn spring-boot:run`

## API

- `GET /api/analytics/requests` returns the collected request logs.
